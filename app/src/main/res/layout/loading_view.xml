<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:tools="http://schemas.android.com/tools"
                android:id="@+id/view_loading_container"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:gravity="center"
                android:padding="8dp"
                android:visibility="gone"
                tools:visibility="visible">
    <TextView
            android:id="@+id/view_loading_label_top"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerHorizontal="true"
            android:textSize="16sp"
            tools:text="Por favor, aguarde" />

    <ProgressBar
            android:id="@+id/view_loading_progress_bar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/view_loading_label_top"
            android:layout_centerHorizontal="true"
            android:layout_margin="16dp"
            android:indeterminate="true"
            android:indeterminateBehavior="cycle" />

    <TextView
            android:id="@+id/view_loading_label_bottom"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/view_loading_progress_bar"
            android:layout_centerHorizontal="true"
            android:textSize="16sp"
            tools:text="Estamos carregando..." />

</RelativeLayout>